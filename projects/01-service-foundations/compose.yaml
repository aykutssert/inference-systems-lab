services:
  service:
    build:
      context: .
    environment:
      APP_ENVIRONMENT: docker
      APP_HOST: 0.0.0.0
      APP_PORT: "8000"
    init: true
    ports:
      - "8000:8000"
    stop_grace_period: 10s
